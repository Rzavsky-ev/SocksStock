# Socks Stock Management System 🧦

## Описание проекта

RESTful API для учета носков на складе с системой ролевой авторизации. Позволяет управлять приходом и расходом носков, а
также предоставляет возможности для административного управления пользователями.

## 🚀 Функциональность

### 🔐 Аутентификация и авторизация

- Регистрация новых пользователей
- Аутентификация по JWT токенам
- Ролевая модель доступа (USER, ADMIN)
- Защищенные эндпоинты с различными уровнями доступа

### 📦 Управление носками

- Приход носков - добавление новых партий на склад
- Расход носков - списание со склада
- Поиск носков - фильтрация по цвету и содержанию хлопка
- Полное удаление - очистка всей базы данных носков (только для администраторов)

### 👥 Управление пользователями

- Просмотр всех пользователей
- Поиск по ID и имени пользователя
- Удаление пользователей

### 🛠 Технологии

- Java 17+
- Spring Boot 3.x
- Spring Security - аутентификация и авторизация
- JWT - JSON Web Tokens
- Spring Data JPA - работа с базой данных
- PostgreSQL 12+ - база данных
- Lombok - сокращение boilerplate кода
- MapStruct - маппинг DTO и сущностей
- Maven - управление зависимостями

## 🚀 Установка и запуск

### Предварительные требования

- Java 17 или выше
- Maven 3.6+
- PostgreSQL 12+

### Шаги установки

1. Настройка базы данных:

```sql
CREATE DATABASE socks_stock;
```

2. Конфигурация приложения (application.properties):

```properties
spring.datasource.url=jdbc:postgresql://localhost:5432/socks_stock
spring.datasource.username=your_username
spring.datasource.password=your_password
app.jwt.secret=your-jwt-secret-key
app.jwt.expiration=86400000
```

3. Сборка и запуск:

```bash
mvn clean install
mvn spring-boot:run
```

## 📚 API Документация

### 🔐 Аутентификация

Вход в систему:

```http
POST /api/auth/login
Content-Type: application/json

{
  "username": "user",
  "password": "password"
}
```

Регистрация пользователя:

```http
POST /api/auth/register?username=user&password=pass
```

Регистрация администратора

```http
POST /api/auth/register-admin?username=admin&password=pass
```

### 🧦 Управление носками

Приход носков

```http
POST /api/socks/income
Content-Type: application/json
Authorization: Bearer <token>

{
"color": "red",
"cottonPart": 80,
"quantity": 100
}
```

Расход носков

```http
POST /api/socks/outcome
Content-Type: application/json
Authorization: Bearer <token>

{
"color": "red",
"cottonPart": 80,
"quantity": 50
}
```

Поиск носков

```http
GET /api/socks?color=red&operation=moreThan&cottonPart=50
Authorization: Bearer <token>
```

### 👥 Административное управление

Получить всех пользователей

```http
GET /api/admin/users
Authorization: Bearer <token>
```

### 🔒 Роли и разрешения

| Роль    | Описание                       |
|---------|--------------------------------|
| `USER`  | Просмотр носков, приход/расход |
| `ADMIN` | Полный доступ ко всем функциям |

### 🗄 Модели данных

Носки (Socks)

```json
{
  "id": "Long",
  "color": "String",
  "cottonPart": "Integer (0-100)",
  "quantity": "Integer"
}
```

Пользователь (AppUser)

```json
{
  "id": "Long",
  "username": "String",
  "password": "String (encoded)",
  "role": "UserRole"
}
```

### 🧪 Тестовые данные

При первом запуске автоматически создается администратор:

```text

Логин: admin
Пароль: admin
```

📝 Операции поиска

- moreThan - содержание хлопка больше указанного

- lessThan - содержание хлопка меньше указанного

- equal - содержание хлопка равно указанному

## 🔐 Безопасность

- Пароли хранятся в зашифрованном виде (BCrypt)

- JWT токены имеют ограниченное время жизни

- CSRF защита отключена (используется stateless JWT)